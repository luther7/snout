language: php

sudo: false

php:
  - 7.1
  - nightly

before_script:
  - pecl install ds
  - composer install --prefer-source --no-interaction

matrix:
  allow_failures:
    - php: nightly
  fast_finish: true

notifications:
  email: false

script: make test
